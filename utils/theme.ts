import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
    palette: {
        primary: {
            main: "rgb(0, 82, 155)",
        },
        secondary: {
            main: "rgb(236, 0, 140)",
        },
    },
});

export default theme;
